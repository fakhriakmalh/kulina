the values from ku_user table is different with EDR. 
in EDR, we can see that ku_user table has ID as a primary key, but from the insertion.sql 
there is no ID value, so then it will be an error, and will effect to the other table